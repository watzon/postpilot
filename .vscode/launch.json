{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Wails: Debug PostPilot",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${workspaceFolder}/build/bin/postpilot",
            "preLaunchTask": "build",
            "cwd": "${workspaceFolder}",
            "env": {}
        }
    ]
}